const tabLinks=document.querySelectorAll('.tab-links');const tabContents=document.querySelectorAll('.tab-contents');function opentab(event,tabname){tabLinks.forEach(tab=>{tab.classList.remove('active-link');tab.setAttribute('aria-selected','false')});tabContents.forEach(content=>content.classList.remove('active-tab'));event.currentTarget.classList.add('active-link');event.currentTarget.setAttribute('aria-selected','true');document.getElementById(tabname).classList.add('active-tab')}const sections=document.querySelectorAll('section[id]');const navLinks=document.querySelectorAll('#sidemenu a');const navbar=document.getElementById('navbar');function updateActiveNav(){let current='';const navHeight=navbar.offsetHeight;sections.forEach(section=>{const sectionTop=section.offsetTop-navHeight-20;const sectionHeight=section.offsetHeight;if(window.scrollY>=sectionTop&&window.scrollY<sectionTop+sectionHeight){current=section.getAttribute('id')}});navLinks.forEach(link=>{link.classList.remove('active');if(link.getAttribute('href')===`#${current}`){link.classList.add('active')}});navbar.classList.toggle('scrolled',window.scrollY>20)}window.addEventListener('scroll',updateActiveNav);window.addEventListener('load',updateActiveNav);document.addEventListener('DOMContentLoaded',()=>{const sidemenu=document.getElementById('sidemenu');const navToggle=document.querySelector('.nav-toggle');const toggleIcon=navToggle?.querySelector('i');if(!sidemenu||!navToggle)return;function setMenuOpen(open){sidemenu.classList.toggle('open',open);document.body.classList.toggle('menu-open',open);sidemenu.setAttribute('aria-hidden',String(!open));navToggle.setAttribute('aria-expanded',String(open));if(toggleIcon){toggleIcon.classList.toggle('fa-bars',!open);toggleIcon.classList.toggle('fa-xmark',open)}}navToggle.addEventListener('click',event=>{event.stopPropagation();setMenuOpen(!sidemenu.classList.contains('open'))});sidemenu.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>setMenuOpen(false)));document.addEventListener('keydown',event=>{if(event.key==='Escape')setMenuOpen(false)});document.addEventListener('click',event=>{if(!sidemenu.classList.contains('open'))return;if(sidemenu.contains(event.target)||navToggle.contains(event.target))return;setMenuOpen(false)})});const revealElements=document.querySelectorAll('.reveal');const revealObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('show');revealObserver.unobserve(entry.target)}})},{threshold:.15});revealElements.forEach(element=>revealObserver.observe(element));const form=document.getElementById('sheetdb-form');if(form){form.addEventListener('submit',async event=>{event.preventDefault();const submitBtn=form.querySelector('button[type="submit"]');const originalText=submitBtn.textContent;submitBtn.textContent='Submitting...';submitBtn.disabled=true;try{const response=await fetch(form.action,{method:'POST',body:new FormData(form)});if(!response.ok)throw new Error('Form submission failed');alert(`Submitted! Thank you ${document.getElementById('uname').value}`);form.reset()}catch(error){alert('Sorry, something went wrong. Please try again later.')}finally{submitBtn.textContent=originalText;submitBtn.disabled=false}})}